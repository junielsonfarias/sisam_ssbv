/**
 * Fachada de compatibilidade do service de KPIs da SEMED.
 *
 * A implementação foi decomposta em submódulos sob `./kpis-semed/`
 * (gerais, frequencia, desempenho, programas, comparativo). Este arquivo
 * preserva o import histórico `@/lib/services/kpis-semed.service`.
 *
 * @module services/kpis-semed.service
 */

export * from './kpis-semed'
