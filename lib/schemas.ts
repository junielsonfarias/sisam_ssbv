/**
 * Barrel file — mantém compatibilidade com imports existentes
 *
 * Schemas foram divididos em módulos por domínio em lib/schemas/
 * Veja lib/schemas/index.ts para o mapa completo.
 */
export * from './schemas/index'
