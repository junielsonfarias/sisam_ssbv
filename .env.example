# Configuracoes do Banco de Dados PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sisam
DB_USER=postgres
DB_PASSWORD=sua_senha_aqui

# Para Supabase em producao, use:
# DB_HOST=aws-0-sa-east-1.pooler.supabase.com (seu host do Supabase)
# DB_PORT=6543  # IMPORTANTE: Use 6543 para Transaction Mode (suporta 50+ usuarios)
#               # Porta 5432 = Session Mode (limitado a ~15 usuarios)
# DB_SSL=true

# Chave secreta para JWT (OBRIGATORIO: altere em producao!)
JWT_SECRET=sua-chave-secreta-aqui-altere-em-producao

# Ambiente
NODE_ENV=development
